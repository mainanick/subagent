{
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/**/*"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/mainanick/subagent.git"
  },
  "engines": {
    "node": ">= v10.17.0"
  },
  "scripts": {
    "build": "yarn workspace subagent run build",
    "start": "yarn workspace subagent run start",
    "dev:http": "yarn workspace @subagent/http run dev",
    "test": "yarn workspace subagent run test",
    "lint": "prettier --write packages/**/*.{ts,tsx,js,jsx}"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^4.2.3",
    "prettier": "^1.19.1",
    "ts-jest": "^25.2.1",
    "ts-node": "^8.6.2",
    "typescript": "^3.8.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint"
    }
  }
}